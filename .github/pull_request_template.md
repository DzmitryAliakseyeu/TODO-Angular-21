## Description

<!-- Required. What does this PR do and why?
     Example: "Adds a filter to hide completed tasks from the list." -->

## Type of change

- [ ] Bug fix — something was broken and now it works
- [ ] New feature — adds new functionality
- [ ] Refactor — code change that doesn't affect behavior
- [ ] Style / UI — visual or layout changes only
- [ ] Docs — README or comments only
- [ ] Config / build — angular.json, netlify.toml, tsconfig, etc.

## What changed

<!-- List every meaningful change. One line per item.
     Example:
     - Added `filterTasks()` method to FormManager service
     - Updated template to use @if for completed/active separation
     - Added `.is-hidden` SCSS class -->

-

## How to test

<!-- Steps to manually verify the change works correctly.
     Example:
     1. Run `ng serve`
     2. Add a task
     3. Click Complete — task should move to the completed section -->

1.

## Screenshots

<!-- Add before / after screenshots for any UI change. Delete this section if not applicable. -->

## Checklist

- [ ] `npm run build` passes with no errors
- [ ] No TypeScript errors in the editor
- [ ] No unintended files included (dist/, node_modules/, .env)
- [ ] Follows the existing code style (SCSS variables, standalone components)
- [ ] README updated if a feature, config, or structure changed
